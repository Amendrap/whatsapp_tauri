{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for WhatsApp Tauri – intentionally minimal.",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
